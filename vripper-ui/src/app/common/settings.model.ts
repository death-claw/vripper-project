export interface Settings {
    downloadPath: string;
    maxThreads: number;
    autoStart: boolean;
    vLogin: boolean;
    vUsername: string;
    vPassword: string;
    vThanks: boolean;
    desktopClipboard: boolean;
  }