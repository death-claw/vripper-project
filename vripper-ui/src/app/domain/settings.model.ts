export interface Settings {
  downloadPath: string;
  maxThreads: number;
  maxTotalThreads: number;
  autoStart: boolean;
  vLogin: boolean;
  vUsername: string;
  vPassword: string;
  vThanks: boolean;
  desktopClipboard: boolean;
  viewPhotos: boolean;
  resolveTitle: boolean;
}
